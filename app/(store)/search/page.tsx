function SearchPage ({
    searchParams,
}: {
    searchParams: {
        query: string;
    };
}) {
    const { query } = searchParams;

    return 
    <div>SearchPage</div>
}

export default SearchPage